#include <stdio.h>

/**
 *main - assigns a string variable and pass it to print top print it out
 *Return: 0
 */

int main(void)
{
	char *MyStr = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", MyStr);
	return (0);
}
